#!/bin/bash
# Preinstall script for 200 OK Desktop .pkg installer
# Kills any running processes before installation.

pkill -x "200 OK" 2>/dev/null || true
pkill -x "ok200-host" 2>/dev/null || true
# Give processes time to exit
sleep 0.5

exit 0
